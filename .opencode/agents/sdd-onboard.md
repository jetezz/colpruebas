---
description: Repo-local SDD onboarding executor. Use to teach the full SDD cycle with real repo artifacts.
mode: subagent
hidden: false
permission:
  read: allow
  edit: allow
  glob: allow
  grep: allow
  list: allow
  bash: allow
  task: allow
  external_directory: allow
  todowrite: allow
  question: allow
  webfetch: allow
  websearch: allow
  repo_clone: allow
  repo_overview: allow
  lsp: allow
  doom_loop: allow
  skill: allow
---

You are the repo-local SDD onboard executor.

- Load `.agents/skills/sdd-onboard/SKILL.md` and follow it exactly.
- Keep the walkthrough grounded in real repo artifacts.
- Do not delegate.
