# APPLY-WU-CRS-2 — Evidencia de implementación (AC-005 CROSS — prereqs documentales)

> Lane: `sdd-apply-doc` · Unit: `WU-CRS-2` · apply_lane: `doc`
> Objetivo: AC-005 — prerrequisitos cross documentales + archivo inicial del skill-registry.
> Estado de la unit: `done` · Archivo de evidencia: este artifact.
> Estándar inyectado: `projectctl-requirements` v10.0.0 / binding `task-flow-binding` v9.0.0.
> Modo: parallel-safe · Conflict group: docs.

---

## 1. Alcance y gate de ownership

| Gate | Resultado | Evidencia |
| --- | --- | --- |
| **Owned files** | PASS | Archivos creados (NEW) exactos del `Archivos owned`: `AGENTS.md`, `README.md`, `docs/00-context/agents_skills.md`, `docs/04-process/task.md`, `.atl/skill-registry.md`. Nada más modificado. |
| **apply_lane** | PASS | `apply_lane: doc` — solo archivos documentales/registro (sin código producto, sin tests). |
| **Spec linkage** | PASS | REQ-CRS-001..005 (ver §5). |
| **No tocar ajenos** | PASS | NO se tocaron `docs/00-context/entornos.md` (WU-ENT-2) ni el bundle `views/projectctl` (WU-DOC-1). |

---

## 2. Implementación

### 2.1 `AGENTS.md` (NEW) — REQ-CRS-001

Directrices de operación de agentes del repo `colpruebas`: propósito del repo, convenciones
(docs funcionales `docs/app-map/**`, registro de skills, testing Bun con `// @ac`, estados solo
del binding), comandos permitidos (`bun run test:check`, `bun`, `projectctl`; sandbox sin
docker), contrato de entrega (rama feature + PR único, env/runtime excluidos del commit) y
referencia al flujo SDD. Declara explícitamente que la SoT normativa del flujo es el bloque
`task-flow-binding` v9.0.0 (anti-drift).

### 2.2 `README.md` (NEW) — REQ-CRS-002

Descripción del proyecto + entry points (`/projectctl`, docs, tests) + cómo correr tests
(`bun run test:check`, runner unificado, `bun test`). Sin duplicar policy de skills.

### 2.3 `docs/00-context/agents_skills.md` (NEW) — REQ-CRS-003

Registro legible de skills, coherente con `.atl/skill-registry.md`. Lista las skills
project-installed (`coordinador`, `engram-policy`, `judgment-day`, `projectctl-requirements`,
`sd-protocol`) y `sandbox-runtime-policy` como **prevista** (se instala en WU-ENT-3). Nombra
`projectctl-requirements` y `sandbox-runtime-policy` (requisito del verify).

### 2.4 `docs/04-process/task.md` (NEW) — REQ-CRS-004

Guía de **cómo operar** el flujo de tareas. CITA el bloque `task-flow-binding` v9.0.0 en
`.agents/skills/projectctl-requirements/references/tareas.md` como única SoT normativa, SIN
publicar tablas duplicadas de fases/estados/lanes/gates (solo prosa descriptiva que remite al
binding). Cumple el requisito anti-drift.

### 2.5 `.atl/skill-registry.md` (NEW, inicial) — REQ-CRS-005

Inventario inicial siguiendo el **formato del generator** (maintenance.md §10): header
`<!-- Auto-generated by gentle-ai skill-registry refresh ... -->`, tabla `| Skill | Trigger /
description | Scope | Path |` con separador `| --- |`, una fila por skill project-installed con
`Scope: project` y path absoluto derivado de `REPO_ROOT`. Excluye las workflow skills `sdd-*`
(contrato del generador). Las skills instaladas de este repo (destino) son 5: `coordinador`,
`engram-policy`, `judgment-day`, `projectctl-requirements`, `sd-protocol`. `sandbox-runtime-policy`
(aún no instalada; WU-ENT-3) se lista en una sub-sección **prevista** para que la regeneración
coordinator-only (WU-REG) la añada como fila al instalarse.

---

## 3. Verificación narrow (implementation-local, docs-only)

Chequeo read-only de los verify expects de la unit:

| Verify expects | Resultado | Evidencia |
| --- | --- | --- |
| Los 5 archivos existen | PASS | `AGENTS.md`, `README.md`, `docs/00-context/agents_skills.md`, `docs/04-process/task.md`, `.atl/skill-registry.md` presentes. |
| `docs/04-process/task.md` ref. `task-flow-binding`/`tareas.md` | PASS | Cita el bloque `task-flow-binding` v9.0.0 y el path `.agents/skills/projectctl-requirements/references/tareas.md` en §1/§3/§Referencias. |
| `docs/04-process/task.md` sin tablas duplicadas de fases/estados/lanes/gates | PASS | Sin tablas de machine values; prosa que remite al binding como única SoT. |
| `agents_skills.md` nombra `projectctl-requirements` (y `sandbox-runtime-policy` prevista) | PASS | grep confirma ambos identificadores. |
| `.atl/skill-registry.md` nombra `projectctl-requirements` (y `sandbox-runtime-policy` prevista) | PASS | fila principal + sub-sección prevista. |

---

## 4. Documentación actualizada

Este WU crea los prerrequisitos cross documentales de AC-005. La status de "documentación
actualizada" queda reflejada por el coordinador en el índice (este lane no escribe el índice).
Archivos documentales tocados (todos NEW, dentro del scope owned): `AGENTS.md`, `README.md`,
`docs/00-context/agents_skills.md`, `docs/04-process/task.md`, `.atl/skill-registry.md`.

---

## 5. Criterios / tasks contract

- `criteria_covered`: **AC-005**
- Spec scenarios satisfechos: REQ-CRS-001, REQ-CRS-002, REQ-CRS-003, REQ-CRS-004, REQ-CRS-005.
- Deviations: NONE. (El generador `gentle-ai skill-registry refresh --force` NO está disponible
  en este repo; se creó el registro **inicial** siguiendo el formato del generator contract, y
  la regeneración formal queda a cargo del coordinador en WU-REG — consistente con design open
  question §9.4 y tasks.md WU-REG.)
- Unresolved follow-up: `.atl/skill-registry.md` se regenerará formalmente (WU-REG) tras la
  instalación de `sandbox-runtime-policy` (WU-ENT-3) y quedará como fuente canónica final.

---

## 6. Delivery risk report

- **Force-add required**: NONE. Los 5 archivos owned viven bajo la superficie de commit normal
  del repo (`AGENTS.md`, `README.md`, `docs/`, `.atl/`); ninguno está gitignored.
- **Exclude from commit**: NONE para este WU. (Los archivos env `.env`/`.env.dev` y `.runtime/`
  pertenecen a otros WUs, no a este.)
- **Policy review required**: ninguno conocido en la superficie de este WU.
- Nota: `.atl/` es un directorio nuevo; el archivo es stageable normal. No se verificó la
  stageabilidad mecánica (autoridad de esta lane es docs-only); el coordinador realiza el check
  final de entrega.

---

## 7. Envelope (index-primary lane)

- `summary`: AC-005 cross documents created (AGENTS.md, README.md, agents_skills.md, task.md,
  .atl/skill-registry.md initial) following the generator registry format and anti-drift
  (task.md cites task-flow-binding v9.0.0 without parallel catalogs).
- `artifact_ref`: `taskReadme/20260825-bhbr8k-remediacion-compatibilidad-projectctl-entornos-docs-tests/apply-WU-CRS-2.md`
- `unit_status`: `done`
- `unit_id`: `WU-CRS-2`
- `doc_files_modified`: `AGENTS.md`, `README.md`, `docs/00-context/agents_skills.md`,
  `docs/04-process/task.md`, `.atl/skill-registry.md`

---

**criteria_covered**: AC-005
**next_recommended**: WU-ENT-1 (`sdd-apply-code-medium`, serial) — env/overlays canónicos, per
AD-10 tras WU-CRS-2 (+WU-DOC-1 parallel-safe).
